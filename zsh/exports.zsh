
# Setup terminal
export TERM="xterm-256color"

# Setup default editor (best to have VISUAL and EDITOR variables in sync.)
export EDITOR="/usr/bin/nano"
export VISUAL="${EDITOR}"
export SVN_EDITOR="${EDITOR}"
